# Arco v0.3.22

- 混合录音默认开启 WebRTC AEC3 软件回声消除，减少外放声音被麦克风重复收录；不调整系统播放音量。
- 原始录音继续保存，回声处理后的音频用于实时转写。
- 修复从桌面启动 Arco 后 Codex CLI 未使用已启用的 macOS 手动代理而超时的问题。没有配置代理时继续直连，显式环境变量优先。

回声消除的效果取决于设备、房间和输入质量。本次未修改说话人编号算法，不保证自动合并所有错误编号。

安装包：Apple Silicon macOS DMG。附 SHA-256 校验文件。
